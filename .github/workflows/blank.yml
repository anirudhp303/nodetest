name: Node CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: pull
	working-directory: ../../nodetest 
        run: |
		git pull
		npm install
		node index.js
