name: Node CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: pul
	working-directory: /home/anirudh/Desktop/server/nodetest 
        run: git pull
      - name: install
        working-directory: /home/anirudh/Desktop/server/nodetest
        run: npm install

      - name: restart 
	working-directory: /home/anirudh/Desktop/server/nodetest
        run: node index.js
